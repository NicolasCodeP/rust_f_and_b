# Mac stuff:
.DS_Store

# trunk output folder (for local development)
# dist is needed for deployment, so we'll only ignore it locally
dist/

# Rust compile target directories:
target
target_ra
target_wasm

# https://github.com/lycheeverse/lychee
.lycheecache
